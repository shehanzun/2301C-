函数默认参数
默认参数固定放到最后面,如果传参时少传参数会显示undefined
用 = 号来赋初始值

arguments伪数组
它没有数组的方法，但有数组的长度属性和下标,打印的是伪数组
里面有传递过来的所有数据

剩余参数
用...args来声明的,args不是固定的,但前面要有...,并把剩余
参数放到最后一位,打印出来是一个数组形式,里面有没和形参对应
的数据，如果有相对应的形参则不会放到这个数组内
